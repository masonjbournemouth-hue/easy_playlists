org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.workers.max=1

android.useAndroidX=true
android.enableJetifier=false

# Replace the removed flag with the supported one:
android.useFullClasspathForDexingTransform=true

# (Optional) quiets warnings about removed options in 8.3+
android.suppressUnsupportedOptionWarnings=android.enableDexingArtifactTransform

kotlin.daemon.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=384m
